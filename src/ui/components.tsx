import { MaterialCommunityIcons } from '@expo/vector-icons';
import React, { useState } from 'react';
import { Image, KeyboardAvoidingView, Modal, Platform, Pressable, ScrollView, StyleProp, StyleSheet, Text, TextInput, TextInputProps, View, ViewStyle } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { useAccounts } from '../context/AccountsContext';
import { shadow, theme } from '../shared/theme';
type Icon = React.ComponentProps<typeof MaterialCommunityIcons>['name'];
export function Screen({ children }: { children: React.ReactNode }) { const inset = useSafeAreaInsets(); return <KeyboardAvoidingView style={s.screen} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><ScrollView keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false} contentContainerStyle={[s.content, { paddingTop: Math.max(inset.top, 14) }]}>{children}</ScrollView></KeyboardAvoidingView>; }
export function Card({ children, style }: { children: React.ReactNode; style?: StyleProp<ViewStyle> }) { return <View style={[s.card, style]}>{children}</View>; }
export function Button({ label, icon, onPress, tone = 'primary', disabled }: { label: string; icon?: Icon; onPress: () => void; tone?: 'primary' | 'ghost' | 'danger' | 'success'; disabled?: boolean }) { const dark = tone === 'primary'; return <Pressable accessibilityRole="button" disabled={disabled} onPress={onPress} style={({ pressed }) => [s.button, s[`button_${tone}`], pressed && s.pressed, disabled && { opacity: .45 }]}>{icon && <MaterialCommunityIcons name={icon} size={19} color={dark ? theme.colors.white : tone === 'danger' ? theme.colors.danger : tone === 'success' ? theme.colors.success : theme.colors.text} />}<Text style={[s.buttonText, dark && { color: theme.colors.white }, tone === 'danger' && { color: theme.colors.danger }, tone === 'success' && { color: theme.colors.success }]}>{label}</Text></Pressable>; }
export function Input(props: TextInputProps & { label?: string }) { return <View>{props.label && <Text style={s.label}>{props.label}</Text>}<TextInput {...props} placeholderTextColor={theme.colors.muted} style={[s.input, props.multiline && s.multiline, props.style]} /></View>; }
export function Header({ title }: { title: string }) { const { profiles, selected, select } = useAccounts(); const [open, setOpen] = useState(false); return <><View style={s.header}><View style={s.brand}><Image source={require('../../assets/iconography/app-icon.png')} style={s.logo} /><Text style={s.hero}>{title}</Text></View><Pressable onPress={() => setOpen(true)} style={s.selector}><Text numberOfLines={1} style={s.selectorText}>{selected?.name ?? 'Cuenta'}</Text><MaterialCommunityIcons name="chevron-down" size={18} color={theme.colors.muted} /></Pressable></View><Sheet visible={open} title="Seleccionar cuenta" onClose={() => setOpen(false)}>{profiles.map(p => <Pressable key={p.id} onPress={() => { select(p.id); setOpen(false); }} style={[s.option, p.id === selected?.id && s.optionActive]}><MaterialCommunityIcons name={p.currency === 'USDT' ? 'currency-usd' : 'cash'} size={22} color={p.id === selected?.id ? theme.colors.success : theme.colors.muted} /><View><Text style={s.optionTitle}>{p.name}</Text><Text style={s.optionSub}>Contabilidad en {p.currency}</Text></View></Pressable>)}</Sheet></>; }
export function Sheet({ visible, title, onClose, children }: { visible: boolean; title: string; onClose: () => void; children: React.ReactNode }) { const inset = useSafeAreaInsets(); return <Modal transparent visible={visible} animationType="fade" onRequestClose={onClose}><View style={s.modal}><Pressable style={StyleSheet.absoluteFill} onPress={onClose} /><View style={[s.sheet, { paddingBottom: Math.max(inset.bottom, 20) }]}><View style={s.handle} /><View style={s.sheetHead}><Text style={s.title}>{title}</Text><Pressable accessibilityLabel="Cerrar" onPress={onClose} style={s.close}><MaterialCommunityIcons name="close" size={21} /></Pressable></View><ScrollView keyboardShouldPersistTaps="handled">{children}</ScrollView></View></View></Modal>; }
export function Tabs<T extends string>({ value, options, onChange }: { value: T; options: { value: T; label: string }[]; onChange: (value: T) => void }) { return <View style={s.tabs}>{options.map(o => <Pressable key={o.value} onPress={() => onChange(o.value)} style={[s.tab, value === o.value && s.tabActive]}><Text style={[s.tabText, value === o.value && s.tabTextActive]}>{o.label}</Text></Pressable>)}</View>; }
export const money = (value: number, currency: string) => `${value < 0 ? '−' : ''}${Math.abs(value).toLocaleString('en-US', { minimumFractionDigits: currency === 'USDT' ? 3 : 0, maximumFractionDigits: currency === 'USDT' ? 3 : 0 })} ${currency}`;
const s = StyleSheet.create({ screen: { flex: 1, backgroundColor: theme.colors.background }, content: { paddingHorizontal: 16, paddingBottom: 116, gap: 16 }, card: { backgroundColor: theme.colors.surface, borderRadius: 24, borderWidth: 1, borderColor: theme.colors.border, padding: 16, ...shadow.card }, header: { minHeight: 44, flexDirection: 'row', alignItems: 'center', gap: 8 }, brand: { flex: 1, flexDirection: 'row', alignItems: 'center', gap: 9 }, logo: { width: 34, height: 34, borderRadius: 11 }, hero: { ...theme.type.hero, color: theme.colors.text, flex: 1 }, selector: { maxWidth: 155, minHeight: 40, borderRadius: 15, backgroundColor: theme.colors.surface, borderWidth: 1, borderColor: theme.colors.border, flexDirection: 'row', alignItems: 'center', paddingHorizontal: 10, gap: 5 }, selectorText: { ...theme.type.caption, fontWeight: '700', flexShrink: 1 }, button: { minHeight: 48, borderRadius: 18, borderWidth: 1, borderColor: theme.colors.border, paddingHorizontal: 16, flexDirection: 'row', gap: 8, alignItems: 'center', justifyContent: 'center' }, button_primary: { backgroundColor: theme.colors.primary }, button_ghost: { backgroundColor: theme.colors.surface }, button_danger: { backgroundColor: theme.colors.dangerSoft }, button_success: { backgroundColor: theme.colors.successSoft }, pressed: { transform: [{ scale: .98 }], opacity: .9 }, buttonText: { ...theme.type.label, color: theme.colors.text }, label: { ...theme.type.caption, color: theme.colors.muted, marginBottom: 7, marginLeft: 3 }, input: { minHeight: 50, borderRadius: 18, backgroundColor: theme.colors.surface, borderWidth: 1, borderColor: theme.colors.border, paddingHorizontal: 15, color: theme.colors.text, ...theme.type.body }, multiline: { minHeight: 82, paddingTop: 13, textAlignVertical: 'top' }, modal: { flex: 1, justifyContent: 'flex-end', backgroundColor: theme.colors.overlay }, sheet: { maxHeight: '90%', backgroundColor: theme.colors.background, paddingHorizontal: 16, paddingTop: 9, borderTopLeftRadius: 30, borderTopRightRadius: 30 }, handle: { width: 44, height: 5, borderRadius: 5, alignSelf: 'center', backgroundColor: '#C5CDD8', marginBottom: 10 }, sheetHead: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginBottom: 16 }, title: { ...theme.type.title }, close: { width: 42, height: 42, borderRadius: 15, backgroundColor: theme.colors.surfaceAlt, alignItems: 'center', justifyContent: 'center' }, option: { minHeight: 62, flexDirection: 'row', alignItems: 'center', gap: 12, padding: 13, borderRadius: 18, marginBottom: 9, backgroundColor: theme.colors.surface, borderWidth: 1, borderColor: theme.colors.border }, optionActive: { backgroundColor: theme.colors.successSoft }, optionTitle: { ...theme.type.label }, optionSub: { ...theme.type.caption, color: theme.colors.muted }, tabs: { flexDirection: 'row', padding: 5, borderRadius: 20, backgroundColor: theme.colors.surface, borderWidth: 1, borderColor: theme.colors.border }, tab: { flex: 1, minHeight: 42, alignItems: 'center', justifyContent: 'center', borderRadius: 15 }, tabActive: { backgroundColor: theme.colors.background, borderWidth: 1, borderColor: theme.colors.border }, tabText: { ...theme.type.caption, color: theme.colors.muted }, tabTextActive: { color: theme.colors.text, fontWeight: '800' } });
